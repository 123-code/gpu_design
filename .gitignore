# FPGA build outputs (regenerated by ./build_fpga.sh)
impl/
*.fs

# iverilog simulation outputs
gpu_sim
mac_sim
sim_loadrun
sim_divergence
sim_divmerge
sim_warps
sim_mac32
sim_mlp
*.vcd

# Open-source build flow generated artifacts (run_oss.sh is kept; *.fs already ignored)
oss_build/tiny_gpu.v
oss_build/*.json
# Per-config sweep/build intermediates (build_cfg.sh / area_sweep.sh outputs; large PnR JSONs)
oss_build/cfg/
oss_build/sweep/

# Rust assembler build
software/target/

# Dumps regenerated by mnist_ref.py (image 0 kept for the FC test / default send)
software/mnist_data/features*.hex
!software/mnist_data/features0.hex
software/mnist_data/fc_payload*.hex
!software/mnist_data/fc_payload0.hex

# J++ / assembler outputs (regenerated by `make run-jpp`)
software/demo.asm
software/demo.hex
software/program.asm
software/program.hex

# macOS
.DS_Store
.gstack/

# Repo digest dumps (regenerable LLM-context exports)
digest.txt
